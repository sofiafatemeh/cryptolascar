---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - config.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04

must_haves:
  truths:
    - "Toutes les variables d'environnement nécessaires sont documentées dans .env.example avec description"
    - "config.py charge le .env via python-dotenv et expose un objet Config importable"
    - "Une variable manquante lève une ValueError avec le nom exact de la variable manquante"
    - "Aucun secret n'est hardcodé dans config.py — uniquement os.getenv() ou dotenv"
    - ".env.example ne contient aucune valeur secrète réelle, uniquement des placeholders"
  artifacts:
    - path: ".env.example"
      provides: "Documentation exhaustive de toutes les variables d'environnement"
      contains: "SMTP_HOST"
    - path: "config.py"
      provides: "Chargement et validation centralisée du .env"
      exports: ["Config", "get_config"]
  key_links:
    - from: "config.py"
      to: ".env"
      via: "python-dotenv load_dotenv()"
      pattern: "load_dotenv"
    - from: "config.py"
      to: "ValueError"
      via: "validation des variables requises"
      pattern: "raise ValueError"
---

<objective>
Créer la couche de configuration centralisée : .env.example documente chaque variable attendue, config.py charge le .env au démarrage et lève une erreur explicite si une variable obligatoire est manquante.

Purpose: Garantir que le système refuse de démarrer avec une configuration incomplète — pas de crash silencieux à l'exécution, pas de secret hardcodé dans le code.
Output: .env.example (référence documentaire), config.py (module de config importable et validé).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Créer .env.example avec toutes les variables documentées</name>
  <files>
    .env.example
  </files>
  <read_first>
    - CLAUDE.md (section "Environment Variables" — liste officielle des variables requises)
  </read_first>
  <action>
Créer `.env.example` à la racine du projet avec le contenu exact suivant.
Chaque variable a un commentaire descriptif. Aucune valeur réelle — uniquement des placeholders.

```
# =============================================================================
# CryptoLascar — Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs réelles
# NE JAMAIS commiter .env dans git
# =============================================================================

# --- SMTP / Email ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_gmail@gmail.com
SMTP_PASSWORD=your_gmail_app_password

# Liste de destinataires (séparés par virgule, sans espaces)
RECIPIENT_LIST=recipient@example.com

# --- Anthropic / Claude LLM ---
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Modèle utilisé pour la synthèse narrative (défaut recommandé : claude-sonnet-4-6)
ANTHROPIC_MODEL=claude-sonnet-4-6

# --- APIs de données financières ---
COINGECKO_API_KEY=your_coingecko_api_key
ALPHA_VANTAGE_KEY=your_alpha_vantage_key
FRED_API_KEY=your_fred_api_key
NEWSAPI_KEY=your_newsapi_key

# --- Base de données SQLite ---
# Chemin relatif depuis la racine du projet
DB_PATH=cryptolascar.db

# --- Logging ---
# Niveau de log : DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# Chemin du fichier de log (optionnel — si vide, log uniquement en stdout)
LOG_FILE=cryptolascar.log
```

NE PAS créer .env (ce fichier est git-ignoré et créé manuellement sur le VPS).
  </action>
  <verify>
    <automated>grep -c "SMTP_HOST" .env.example &amp;&amp; grep -c "ANTHROPIC_API_KEY" .env.example &amp;&amp; grep -c "RECIPIENT_LIST" .env.example &amp;&amp; grep -c "DB_PATH" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SMTP_HOST" .env.example` retourne 1 ligne avec valeur placeholder (pas une vraie IP/host)
    - `grep "SMTP_PASSWORD" .env.example` retourne 1 ligne
    - `grep "RECIPIENT_LIST" .env.example` retourne 1 ligne
    - `grep "ANTHROPIC_API_KEY" .env.example` retourne 1 ligne avec valeur placeholder (pas une vraie clé commençant par sk-ant-)
    - `grep "ANTHROPIC_MODEL" .env.example` retourne 1 ligne avec valeur `claude-sonnet-4-6`
    - `grep "COINGECKO_API_KEY" .env.example` retourne 1 ligne
    - `grep "ALPHA_VANTAGE_KEY" .env.example` retourne 1 ligne
    - `grep "FRED_API_KEY" .env.example` retourne 1 ligne
    - `grep "NEWSAPI_KEY" .env.example` retourne 1 ligne
    - `grep "DB_PATH" .env.example` retourne 1 ligne avec valeur `cryptolascar.db`
    - `grep "LOG_LEVEL" .env.example` retourne 1 ligne avec valeur `INFO`
    - Aucune valeur réelle (clé API, mot de passe) dans le fichier — vérifier visuellement
  </acceptance_criteria>
  <done>.env.example documente les 12+ variables avec placeholders et commentaires descriptifs. Peut être copié-collé sur le VPS comme base de configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : Créer config.py avec chargement et validation du .env</name>
  <files>
    config.py
    tests/test_config.py
  </files>
  <read_first>
    - .env.example (source de vérité pour la liste des variables à valider)
    - CLAUDE.md (section "Key Constraints" — "Zéro credential hardcodé")
  </read_first>
  <behavior>
    - Test 1 : `get_config()` avec un .env complet retourne un objet Config sans lever d'exception
    - Test 2 : `get_config()` avec SMTP_HOST manquant lève `ValueError` contenant la chaîne "SMTP_HOST"
    - Test 3 : `get_config()` avec ANTHROPIC_API_KEY manquant lève `ValueError` contenant "ANTHROPIC_API_KEY"
    - Test 4 : `config.smtp_host` retourne la valeur définie dans l'environnement de test
    - Test 5 : `config.recipient_list` retourne une liste Python (pas une string) même si .env contient une chaîne séparée par virgules
    - Test 6 : `config.anthropic_model` retourne `"claude-sonnet-4-6"` quand ANTHROPIC_MODEL n'est pas défini (valeur par défaut)
  </behavior>
  <action>
Créer `config.py` à la racine du projet avec le contenu suivant :

```python
"""
config.py — Chargement et validation centralisée de la configuration.
Toutes les variables proviennent de .env via python-dotenv.
Aucun secret hardcodé dans ce fichier.
"""
from __future__ import annotations

import os
from dataclasses import dataclass, field
from pathlib import Path
from typing import List

from dotenv import load_dotenv


def _require(name: str) -> str:
    """Retourne la valeur de la variable d'environnement ou lève ValueError."""
    value = os.getenv(name)
    if not value:
        raise ValueError(
            f"Variable d'environnement manquante ou vide : {name}. "
            f"Vérifier .env (voir .env.example pour la liste complète)."
        )
    return value


def _optional(name: str, default: str = "") -> str:
    """Retourne la valeur de la variable ou la valeur par défaut."""
    return os.getenv(name, default)


@dataclass
class Config:
    """Configuration complète du système CryptoLascar."""

    # SMTP
    smtp_host: str
    smtp_port: int
    smtp_user: str
    smtp_password: str
    recipient_list: List[str]

    # LLM
    anthropic_api_key: str
    anthropic_model: str

    # APIs de données
    coingecko_api_key: str
    alpha_vantage_key: str
    fred_api_key: str
    newsapi_key: str

    # Storage
    db_path: Path

    # Logging
    log_level: str
    log_file: str


def get_config(env_file: str = ".env") -> Config:
    """
    Charge le fichier .env et retourne un objet Config validé.
    Lève ValueError si une variable obligatoire est absente.

    Args:
        env_file: Chemin vers le fichier .env (défaut : ".env" à la racine)

    Returns:
        Config: objet de configuration complet et validé

    Raises:
        ValueError: si une variable obligatoire est manquante ou vide
    """
    load_dotenv(env_file, override=False)

    # Variables SMTP obligatoires
    smtp_host = _require("SMTP_HOST")
    smtp_port_str = _require("SMTP_PORT")
    smtp_user = _require("SMTP_USER")
    smtp_password = _require("SMTP_PASSWORD")
    recipient_raw = _require("RECIPIENT_LIST")

    # Variables LLM obligatoires
    anthropic_api_key = _require("ANTHROPIC_API_KEY")
    anthropic_model = _optional("ANTHROPIC_MODEL", "claude-sonnet-4-6")

    # APIs de données obligatoires
    coingecko_api_key = _require("COINGECKO_API_KEY")
    alpha_vantage_key = _require("ALPHA_VANTAGE_KEY")
    fred_api_key = _require("FRED_API_KEY")
    newsapi_key = _require("NEWSAPI_KEY")

    # Storage
    db_path_str = _optional("DB_PATH", "cryptolascar.db")

    # Logging
    log_level = _optional("LOG_LEVEL", "INFO")
    log_file = _optional("LOG_FILE", "")

    # Conversions de types
    try:
        smtp_port = int(smtp_port_str)
    except ValueError:
        raise ValueError(f"SMTP_PORT doit être un entier, reçu : {smtp_port_str!r}")

    recipient_list = [r.strip() for r in recipient_raw.split(",") if r.strip()]
    if not recipient_list:
        raise ValueError("RECIPIENT_LIST ne peut pas être vide.")

    return Config(
        smtp_host=smtp_host,
        smtp_port=smtp_port,
        smtp_user=smtp_user,
        smtp_password=smtp_password,
        recipient_list=recipient_list,
        anthropic_api_key=anthropic_api_key,
        anthropic_model=anthropic_model,
        coingecko_api_key=coingecko_api_key,
        alpha_vantage_key=alpha_vantage_key,
        fred_api_key=fred_api_key,
        newsapi_key=newsapi_key,
        db_path=Path(db_path_str),
        log_level=log_level.upper(),
        log_file=log_file,
    )
```

Créer aussi `tests/__init__.py` (vide) et `tests/test_config.py` avec les tests suivants :

```python
"""Tests unitaires pour config.py"""
import os
import pytest
from unittest.mock import patch


FULL_ENV = {
    "SMTP_HOST": "smtp.gmail.com",
    "SMTP_PORT": "587",
    "SMTP_USER": "test@gmail.com",
    "SMTP_PASSWORD": "secret",
    "RECIPIENT_LIST": "a@example.com,b@example.com",
    "ANTHROPIC_API_KEY": "sk-ant-test",
    "ANTHROPIC_MODEL": "claude-sonnet-4-6",
    "COINGECKO_API_KEY": "cg-test",
    "ALPHA_VANTAGE_KEY": "av-test",
    "FRED_API_KEY": "fred-test",
    "NEWSAPI_KEY": "news-test",
}


def _get_config_with_env(extra_env: dict = None, remove_keys: list = None):
    """Helper : charge get_config() avec un environnement contrôlé."""
    from config import get_config
    env = dict(FULL_ENV)
    if extra_env:
        env.update(extra_env)
    if remove_keys:
        for k in remove_keys:
            env.pop(k, None)
    # On passe un fichier .env inexistant pour forcer l'usage des vars d'env mockées
    with patch.dict(os.environ, env, clear=True):
        return get_config(env_file=".env.nonexistent")


def test_get_config_full_env_no_exception():
    """Un .env complet ne lève aucune exception."""
    config = _get_config_with_env()
    assert config is not None


def test_missing_smtp_host_raises_valueerror():
    """SMTP_HOST manquant lève ValueError mentionnant SMTP_HOST."""
    with pytest.raises(ValueError, match="SMTP_HOST"):
        _get_config_with_env(remove_keys=["SMTP_HOST"])


def test_missing_anthropic_api_key_raises_valueerror():
    """ANTHROPIC_API_KEY manquant lève ValueError mentionnant ANTHROPIC_API_KEY."""
    with pytest.raises(ValueError, match="ANTHROPIC_API_KEY"):
        _get_config_with_env(remove_keys=["ANTHROPIC_API_KEY"])


def test_smtp_host_value():
    """config.smtp_host retourne la valeur du .env."""
    config = _get_config_with_env()
    assert config.smtp_host == "smtp.gmail.com"


def test_recipient_list_is_list():
    """recipient_list est une liste Python, pas une string."""
    config = _get_config_with_env()
    assert isinstance(config.recipient_list, list)
    assert len(config.recipient_list) == 2
    assert "a@example.com" in config.recipient_list


def test_anthropic_model_default():
    """ANTHROPIC_MODEL absent => valeur par défaut claude-sonnet-4-6."""
    config = _get_config_with_env(remove_keys=["ANTHROPIC_MODEL"])
    assert config.anthropic_model == "claude-sonnet-4-6"
```

Créer le répertoire `tests/` et son `__init__.py` si absents.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_config.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_config.py -v` retourne `6 passed` (ou plus)
    - `grep "load_dotenv" config.py` retourne 1 ligne
    - `grep "raise ValueError" config.py` retourne au moins 2 lignes (dans _require et smtp_port)
    - `grep -v "^#\|^$" config.py | grep -i "password\s*=" | grep -v "os\.getenv\|_require\|smtp_password"` retourne 0 ligne (pas de mot de passe hardcodé)
    - `grep "def get_config" config.py` retourne 1 ligne
    - `python3 -c "from config import get_config, Config; print('OK')"` s'exécute sans ImportError
  </acceptance_criteria>
  <done>config.py est importable. get_config() valide toutes les variables obligatoires et lève ValueError avec le nom exact de la variable manquante. Les 6 tests passent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → config.py | Variables d'environnement traversent python-dotenv avant d'atteindre le code |
| config.py → modules downstream | Config validée — aucune variable non vérifiée ne passe cette frontière |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | config.py | mitigate | Aucune variable sensible loguée ou affichée — uniquement les noms en cas d'erreur |
| T-01-04 | Information Disclosure | .env.example | mitigate | Valeurs placeholder uniquement — aucune clé API réelle commitable |
| T-01-05 | Tampering | config.py | mitigate | `load_dotenv(override=False)` — les variables déjà définies dans l'environnement système prennent la priorité (sécurité VPS) |
| T-01-06 | Spoofing | RECIPIENT_LIST | accept | Liste contrôlée par l'opérateur VPS via .env — pas d'input utilisateur externe |
</threat_model>

<verification>
Depuis la racine du projet :

```bash
# 1. .env.example documenté
grep -E "SMTP_HOST|ANTHROPIC_API_KEY|RECIPIENT_LIST|DB_PATH" .env.example

# 2. config.py importable
python3 -c "from config import get_config, Config; print('Import OK')"

# 3. Validation fonctionne (avec .env inexistant = variables manquantes)
python3 -c "
import os
os.environ.clear()
from config import get_config
try:
    get_config('.env.nonexistent')
except ValueError as e:
    print(f'ValueError attendue : {e}')
"

# 4. Tests passent
python3 -m pytest tests/test_config.py -v

# 5. Aucun secret dans le code
grep -r "sk-ant-\|smtp_password\s*=" config.py || echo "Aucun secret hardcodé OK"
```
</verification>

<success_criteria>
- .env.example documente les 12 variables (SMTP x4, RECIPIENT_LIST, ANTHROPIC x2, APIs x4, DB_PATH, LOG_LEVEL, LOG_FILE)
- config.py charge le .env via load_dotenv() et valide les variables obligatoires
- Une variable manquante lève ValueError avec le nom de la variable dans le message
- 6 tests unitaires passent dans tests/test_config.py
- Aucun credential dans config.py ou .env.example
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
