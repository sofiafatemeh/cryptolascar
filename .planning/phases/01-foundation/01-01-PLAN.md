---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - collectors/__init__.py
  - reporters/__init__.py
  - delivery/__init__.py
  - scheduler/__init__.py
  - db/__init__.py
  - templates/.gitkeep
  - reports/daily/.gitkeep
  - reports/weekly/.gitkeep
  - reports/monthly/.gitkeep
  - tweets/.gitkeep
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Tous les répertoires du projet existent selon la spec CLAUDE.md"
    - "requirements.txt liste toutes les dépendances avec versions minimales"
    - "Les fichiers .gitkeep permettent à git de tracer les répertoires vides"
    - ".gitignore exclut .env, *.db, __pycache__, .pyc"
    - "Chaque sous-module a un __init__.py vide (importable sans erreur)"
  artifacts:
    - path: "requirements.txt"
      provides: "Dépendances Python du projet"
      contains: "python-dotenv"
    - path: ".gitignore"
      provides: "Exclusion des secrets et artefacts générés"
      contains: ".env"
    - path: "collectors/__init__.py"
      provides: "Package Python collectors"
    - path: "db/__init__.py"
      provides: "Package Python db"
  key_links:
    - from: "requirements.txt"
      to: "pip install -r requirements.txt"
      via: "installation VPS"
      pattern: "python-dotenv"
---

<objective>
Mettre en place le squelette complet du projet : arborescence de répertoires, fichiers __init__.py des packages, requirements.txt avec toutes les dépendances v1, et .gitignore sécurisé.

Purpose: Fournir la structure physique sur laquelle tous les modules suivants s'appuient. Sans cette base, les imports Python échouent et aucun module downstream ne peut démarrer.
Output: Arborescence complète, requirements.txt installable, .gitignore fonctionnel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Créer l'arborescence de répertoires et les fichiers __init__.py</name>
  <files>
    collectors/__init__.py
    reporters/__init__.py
    delivery/__init__.py
    scheduler/__init__.py
    db/__init__.py
    templates/.gitkeep
    reports/daily/.gitkeep
    reports/weekly/.gitkeep
    reports/monthly/.gitkeep
    tweets/.gitkeep
  </files>
  <read_first>
    - CLAUDE.md (section "Directory Structure" — source de vérité pour l'arborescence exacte)
  </read_first>
  <action>
Créer tous les répertoires et fichiers suivants à la racine du projet :

**Packages Python (fichiers __init__.py vides) :**
- `collectors/__init__.py` — contenu : `""` (vide, 0 ligne ou commentaire minimal)
- `reporters/__init__.py` — contenu : vide
- `delivery/__init__.py` — contenu : vide
- `scheduler/__init__.py` — contenu : vide
- `db/__init__.py` — contenu : vide

**Répertoires de données (fichiers .gitkeep pour que git les trace) :**
- `templates/.gitkeep` — fichier vide
- `reports/daily/.gitkeep` — fichier vide
- `reports/weekly/.gitkeep` — fichier vide
- `reports/monthly/.gitkeep` — fichier vide
- `tweets/.gitkeep` — fichier vide

Tous les fichiers __init__.py doivent être de vrais fichiers Python valides (vides ou avec un commentaire `# package`), PAS de simples fichiers texte.

NE PAS créer encore : main.py, config.py, db/cache.py (ce sont les plans 02 et 03).
  </action>
  <verify>
    <automated>python3 -c "import collectors; import reporters; import delivery; import scheduler; import db; print('OK')" 2>&amp;&amp; ls reports/daily/.gitkeep reports/weekly/.gitkeep reports/monthly/.gitkeep tweets/.gitkeep templates/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import collectors"` ne lève aucune ImportError
    - `python3 -c "import reporters"` ne lève aucune ImportError
    - `python3 -c "import delivery"` ne lève aucune ImportError
    - `python3 -c "import scheduler"` ne lève aucune ImportError
    - `python3 -c "import db"` ne lève aucune ImportError
    - `ls reports/daily/.gitkeep` retourne le fichier sans erreur
    - `ls tweets/.gitkeep` retourne le fichier sans erreur
    - `ls templates/.gitkeep` retourne le fichier sans erreur
  </acceptance_criteria>
  <done>Les 5 packages Python sont importables et les 5 répertoires de données existent avec .gitkeep.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Créer requirements.txt et .gitignore</name>
  <files>
    requirements.txt
    .gitignore
  </files>
  <read_first>
    - CLAUDE.md (section "Tech Stack" — liste officielle des dépendances)
  </read_first>
  <action>
**Créer `requirements.txt`** avec les dépendances suivantes (versions minimales compatibles Python 3.11+) :

```
# Configuration
python-dotenv>=1.0.0

# HTTP
httpx>=0.27.0

# Data processing
pandas>=2.2.0

# Finance data
yfinance>=0.2.40
pycoingecko>=3.1.0

# LLM
anthropic>=0.30.0

# Email templates
Jinja2>=3.1.4

# Web scraping
beautifulsoup4>=4.12.3
playwright>=1.44.0

# Logging structuré
structlog>=24.1.0

# Scheduling
APScheduler>=3.10.4

# Database (stdlib sqlite3 utilisé directement — pas de SQLAlchemy en v1)
# SQLAlchemy non requis en Phase 1 : sqlite3 standard library suffit
```

**Créer `.gitignore`** avec le contenu suivant :

```
# Secrets — JAMAIS commiter
.env

# Base de données SQLite
*.db
*.sqlite
*.sqlite3

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.pyo
.pytest_cache/

# Environnements virtuels
.venv/
venv/
env/
ENV/

# Rapports générés (optionnel selon config)
# reports/  <- ne pas ignorer, on veut versionner les archives

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Distribution
dist/
build/
*.egg-info/
```

Ne PAS inclure de credential hardcodé dans aucun fichier.
  </action>
  <verify>
    <automated>grep -c "python-dotenv" requirements.txt &amp;&amp; grep -c "\.env" .gitignore &amp;&amp; grep -c "\*\.db" .gitignore &amp;&amp; grep -c "__pycache__" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep "python-dotenv" requirements.txt` retourne exactement 1 ligne (avec version >=1.0.0)
    - `grep "httpx" requirements.txt` retourne 1 ligne
    - `grep "yfinance" requirements.txt` retourne 1 ligne
    - `grep "anthropic" requirements.txt` retourne 1 ligne
    - `grep "structlog" requirements.txt` retourne 1 ligne
    - `grep "APScheduler" requirements.txt` retourne 1 ligne
    - `grep -v '^#' .gitignore | grep '\.env'` retourne `.env` (pas commenté)
    - `grep '*.db' .gitignore` retourne la ligne (*.db git-ignoré)
    - `grep '__pycache__' .gitignore` retourne la ligne
    - Le fichier requirements.txt ne contient aucune clé API, mot de passe ou secret
  </acceptance_criteria>
  <done>requirements.txt liste toutes les dépendances v1 avec versions minimales. .gitignore exclut .env, *.db et __pycache__.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichiers sources → git | .env et *.db ne doivent jamais franchir cette frontière |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore | mitigate | Vérifier que `.env` est listé sans commentaire dans .gitignore avant tout commit |
| T-01-02 | Information Disclosure | requirements.txt | accept | Dépendances publiques — pas de secret possible dans ce fichier |
</threat_model>

<verification>
Depuis la racine du projet :

```bash
# 1. Packages importables
python3 -c "import collectors, reporters, delivery, scheduler, db; print('Tous les packages OK')"

# 2. Répertoires de données présents
ls reports/daily/ reports/weekly/ reports/monthly/ tweets/ templates/

# 3. requirements.txt cohérent
grep -E "python-dotenv|httpx|pandas|yfinance|anthropic|structlog|APScheduler" requirements.txt

# 4. .gitignore protège les secrets
grep -v '^#' .gitignore | grep '\.env'
```
</verification>

<success_criteria>
- Arborescence complète conforme à CLAUDE.md (tous les répertoires et packages existent)
- `pip install -r requirements.txt` ne produit aucune erreur de résolution
- .gitignore exclut .env, *.db, __pycache__
- Aucun secret ou credential dans aucun fichier commité
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
