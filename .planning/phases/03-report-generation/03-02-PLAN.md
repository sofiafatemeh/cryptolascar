---
phase: 03-report-generation
plan: "03-02"
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - reporters/daily.py
  - tests/test_daily.py
autonomous: true
requirements:
  - REPT-01
must_haves:
  truths:
    - "Appeler `build_daily_report(data)` avec un dict de fixtures retourne une chaîne ≥ 250 mots et ≤ 400 mots"
    - "Le rapport contient exactement 6 sections : Macro Snapshot, ETF Radar, Crypto Pulse, PEA Alert, News Feed, One Signal"
    - "Chaque section narrative est générée via `reporters.base.synthesize_section` (mockable, donc isolable en test)"
    - "Si `synthesize_section` retourne le FALLBACK_TEMPLATE, le rapport reste structuré (toujours 6 sections, jamais d'exception propagée)"
  artifacts:
    - path: "reporters/daily.py"
      provides: "Daily report builder ~300 mots, 6 sections"
      exports: ["build_daily_report"]
    - path: "tests/test_daily.py"
      provides: "Tests TDD pour reporters/daily.py"
      contains: "def test_build_daily_report"
  key_links:
    - from: "reporters/daily.py"
      to: "reporters.base.synthesize_section"
      via: "import direct"
      pattern: "from reporters\\.base import"
    - from: "reporters/daily.py"
      to: "data dict (from collect_all)"
      via: "argument data des sous-clés etf/crypto/pea/macro/news"
      pattern: "data\\[['\"](etf|crypto|pea|macro|news)['\"]\\]"
---

<objective>
Construire `reporters/daily.py` qui prend le dict de données issu de `collect_all()` et retourne un rapport quotidien d'environ 300 mots structuré en 6 sections : Macro Snapshot, ETF Radar, Crypto Pulse, PEA Alert, News Feed, One Signal. Les sections narratives sont déléguées au client `synthesize_section` (Plan 03-01). Le rapport doit rester complet et bien formé même si l'API Claude est temporairement indisponible (dégradation gracieuse via fallback).

Purpose: Le Daily Report est le format le plus fréquent (lun-sam, 6 jours/semaine). Sa fiabilité conditionne la valeur quotidienne du système pour l'utilisateur. La structure 6-sections est dictée par REPT-01 dans REQUIREMENTS.md.

Output: `reporters/daily.py` testé (RED+GREEN), exposant `build_daily_report(data: dict) -> str`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@tests/test_integration.py
@reporters/base.py

<interfaces>
<!-- Contrat de données venant de collect_all() — figé en Phase 2 -->

From tests/test_integration.py (MOCK_*_OK structures):
```python
# data["etf"]
{"tickers": {"SPY": {"price": 500.0, "prev_close": 495.0, "pct_change": 1.01, "volume": 1_000_000}, ...},
 "alpha_vantage_failed": False, "partial": False, "source_used": "yfinance_etf"}

# data["crypto"]
{"coins": {"bitcoin": {"price": 60000.0, "market_cap": ..., "volume_24h": ..., "pct_change_24h": 2.5, "symbol": "BTC"}, ...},
 "fear_greed": {"value": 65, "label": "Greed"},
 "coingecko_failed": False, "fear_greed_failed": False, "partial": False, "source_used": "coingecko"}

# data["pea"]
{"prices": {"CW8.PA": {"price": 350.0, "prev_close": 348.0, "pct_change": 0.57, "volume": 10_000}, ...},
 "eligibility": {"CW8.PA": {"eligible": True, "isin": "LU1681043599"}, ...},
 "eligibility_changed": False, "partial": False, "source_used": "yfinance_pea"}

# data["macro"]
{"series": {"DGS10": {"value": 4.45, "date": "2026-05-09", "series_id": "DGS10"}, ...},
 "fred_failed": False, "partial": False, "source_used": "fred"}

# data["news"]
{"headlines": [{"title": "...", "url": "...", "source": "...", "published_at": "..."}, ...],
 "count": N, "newsapi_failed": False, "scrape_failed": False, "partial": False, "source_used": "news"}

# data["_meta"]
{"sources_ok": [...], "sources_failed": [...], "collected_at": "2026-05-09T..."}
```

From reporters/base.py (Plan 03-01 — déjà construit):
```python
def synthesize_section(prompt: str, config: Config, system: str = "", max_tokens: int = 1024) -> str
def build_section(title: str, body: str) -> str  # retourne "## {title}\n\n{body}\n"
def format_pct(value: float) -> str
def format_currency(value: float, symbol: str = "$") -> str
FALLBACK_TEMPLATE = "[Section indisponible — synthèse Claude temporairement indisponible.]"
```

Sections requises par REPT-01 (dans cet ordre) :
1. Macro Snapshot — taux DGS10/DGS2, inflation CPI, masse M2 (data["macro"]["series"])
2. ETF Radar — prix et variations SPY/QQQ/IWDA.AS/EIMI.AS/CSPX.AS (data["etf"]["tickers"])
3. Crypto Pulse — top coins + Fear & Greed (data["crypto"]["coins"], data["crypto"]["fear_greed"])
4. PEA Alert — variations PEA + alerte si eligibility_changed (data["pea"])
5. News Feed — top 5-7 titres (data["news"]["headlines"][:7])
6. One Signal — synthèse narrative finale (action / point d'attention du jour)

Cible : ~300 mots total. Avec 6 sections, ~50 mots par section en moyenne (fallback templates inclus si API down).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Écrire les tests TDD pour reporters/daily.py</name>
  <files>tests/test_daily.py</files>
  <read_first>
    - tests/test_integration.py (les MOCK_*_OK servent de fixtures de données — les copier ou les importer)
    - tests/test_macro.py (pattern fixture tmp_config)
    - reporters/base.py (signature de synthesize_section, FALLBACK_TEMPLATE)
  </read_first>
  <behavior>
    - Test 1 (test_build_daily_report_returns_string_with_six_sections): mocker `synthesize_section` pour retourner `"NARRATION OK"` ; appeler `build_daily_report(data, config)` avec data complet ; assert que la chaîne retournée contient les 6 titres exacts : `"## Macro Snapshot"`, `"## ETF Radar"`, `"## Crypto Pulse"`, `"## PEA Alert"`, `"## News Feed"`, `"## One Signal"`.
    - Test 2 (test_build_daily_report_word_count_in_target_range): même mock que Test 1 ; assert que `len(report.split())` est entre 250 et 400 (cible ~300 mots — tolérance ±33%). Astuce : le mock `synthesize_section` doit retourner du texte filler suffisamment long (~50 mots) pour atteindre la cible avec 6 sections.
    - Test 3 (test_build_daily_report_calls_synthesize_section): patch `reporters.daily.synthesize_section` (ou réimport) ; assert que `synthesize_section` est appelé au moins une fois (preuve que la narration passe par le client Claude, pas du texte hardcodé).
    - Test 4 (test_build_daily_report_uses_configured_model_via_config): le mock `synthesize_section` capture `kwargs["config"]` ; assert que la `Config` passée a bien `anthropic_model="claude-sonnet-4-6"` (preuve LLM-02 — le modèle est configurable, lu depuis Config).
    - Test 5 (test_build_daily_report_graceful_when_synthesize_returns_fallback): mocker `synthesize_section` pour retourner `FALLBACK_TEMPLATE` ; assert que le rapport contient quand même les 6 titres de section (structure préservée même en mode dégradé) et que `build_daily_report` ne lève aucune exception.
    - Test 6 (test_build_daily_report_handles_missing_subsections): passer un `data` où `data["macro"]` est `{"error": "FRED down", "source_failed": True}` ; assert que la section Macro Snapshot existe quand même dans la sortie (avec un texte indiquant "données indisponibles") et que les autres sections sont normales.
    - Test 7 (test_build_daily_report_includes_pea_alert_when_eligibility_changed): passer `data["pea"]["eligibility_changed"] = True` ; assert que la section PEA Alert contient le mot-clé `"alerte"` ou `"changement"` (signal explicite à l'utilisateur).
  </behavior>
  <action>
    Créer `tests/test_daily.py`. Réutiliser une fixture `tmp_config()` style `test_macro.py`. Importer ou copier les fixtures `MOCK_ETF_OK`, `MOCK_CRYPTO_OK`, `MOCK_PEA_OK`, `MOCK_MACRO_OK`, `MOCK_NEWS_OK` depuis `tests/test_integration.py` (`from tests.test_integration import MOCK_ETF_OK, ...` si pytest le permet, sinon les redéclarer).

    Construire un `data_complete` fixture combinant les 5 mocks + `_meta`.

    Imports : `from reporters.daily import build_daily_report` (ImportError attendu en RED).

    Pour les tests qui mockent `synthesize_section`, utiliser `patch("reporters.daily.synthesize_section")` (le test inspecte le module qui consomme, pas la définition).

    Pour Test 2, le mock doit retourner une chaîne de ~50 mots (par exemple : `"Lorem ipsum " * 25`) pour que la somme atteigne la fourchette [250, 400].

    Pour Test 4, utiliser `mock.call_args_list` pour capturer les `kwargs` ou positional args et vérifier le `Config` passé.

    Lancer `python3 -m pytest tests/test_daily.py -v` après écriture — la suite doit échouer avec ImportError (gate RED).

    Commit avec message : `test(03-02): add failing tests for reporters/daily.py`.
  </action>
  <verify>
    <automated>cd /home/crypton/cryptolascar &amp;&amp; python3 -m pytest tests/test_daily.py -v 2>&amp;1 | grep -E "ImportError|ModuleNotFoundError|collected 7"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `tests/test_daily.py` existe : `test -f tests/test_daily.py` exit 0
    - Au moins 7 tests sont déclarés : `grep -c "^def test_" tests/test_daily.py` >= 7
    - L'import du symbole cible est présent : `grep -c "from reporters.daily import build_daily_report" tests/test_daily.py` >= 1
    - Les 6 titres de section sont vérifiés dans les tests (pour preuve que Task 2 doit les produire) : `grep -cE "Macro Snapshot|ETF Radar|Crypto Pulse|PEA Alert|News Feed|One Signal" tests/test_daily.py` >= 6
    - Les tests échouent en mode RED : `python3 -m pytest tests/test_daily.py 2>&amp;1 | grep -E "ModuleNotFoundError|ImportError"` exit 0
    - Le commit RED existe : `git log --oneline -1 | grep -E "test\\(03-02\\)"` exit 0
  </acceptance_criteria>
  <done>tests/test_daily.py contient 7 tests qui échouent tous avec ImportError ; commit `test(03-02): ...` créé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implémenter reporters/daily.py</name>
  <files>reporters/daily.py</files>
  <read_first>
    - tests/test_daily.py (contrat exact à satisfaire — créé en Task 1)
    - reporters/base.py (synthesize_section, build_section, format_pct, format_currency)
    - tests/test_integration.py (structure MOCK_*_OK pour comprendre data shape)
    - collectors/news.py (pour comprendre les champs des headlines)
  </read_first>
  <action>
    Créer `reporters/daily.py` avec une fonction publique `build_daily_report(data: dict, config: Config) -> str`.

    Squelette imposé :

    ```python
    """
    reporters/daily.py — Daily Report builder (~300 mots, 6 sections).

    Sections (ordre figé par REPT-01) :
      1. Macro Snapshot   — taux & inflation
      2. ETF Radar        — performance ETFs
      3. Crypto Pulse     — top coins + Fear & Greed
      4. PEA Alert        — PEA France + alerte éligibilité
      5. News Feed        — top headlines
      6. One Signal       — point d'attention du jour
    """
    from __future__ import annotations
    from config import Config
    from reporters.base import synthesize_section, build_section, format_pct, format_currency
    from logging_setup import get_logger

    logger = get_logger(__name__)

    DAILY_SYSTEM_PROMPT = (
        "Tu es un analyste financier français. Réponds en français, ton sobre et factuel, "
        "synthèse d'environ 50 mots par section. Pas de conseil financier explicite."
    )

    def _macro_section(data: dict, config: Config) -> str:
        macro = data.get("macro") or {}
        if macro.get("source_failed"):
            return build_section("Macro Snapshot", "Données macro indisponibles ce matin (source FRED en échec).")
        series = macro.get("series", {})
        # construire un résumé chiffré : DGS10, DGS2, CPI, M2
        facts = []
        for key in ("DGS10", "DGS2", "CPIAUCSL", "M2SL"):
            s = series.get(key)
            if s and s.get("value") is not None:
                facts.append(f"{key}={s['value']}")
        prompt = f"Résume en ~50 mots la situation macro ce matin. Données: {', '.join(facts) or 'aucune donnée'}."
        body = synthesize_section(prompt, config=config, system=DAILY_SYSTEM_PROMPT)
        return build_section("Macro Snapshot", body)

    def _etf_section(data: dict, config: Config) -> str:
        etf = data.get("etf") or {}
        if etf.get("source_failed"):
            return build_section("ETF Radar", "Données ETF indisponibles ce matin.")
        tickers = etf.get("tickers", {})
        facts = []
        for sym, t in tickers.items():
            if t.get("price") is not None and t.get("pct_change") is not None:
                facts.append(f"{sym} {format_currency(t['price'])} ({format_pct(t['pct_change'])})")
        prompt = f"Résume en ~50 mots la performance ETFs. Données: {', '.join(facts) or 'aucune donnée'}."
        body = synthesize_section(prompt, config=config, system=DAILY_SYSTEM_PROMPT)
        return build_section("ETF Radar", body)

    def _crypto_section(data: dict, config: Config) -> str:
        crypto = data.get("crypto") or {}
        if crypto.get("source_failed"):
            return build_section("Crypto Pulse", "Données crypto indisponibles ce matin.")
        coins = crypto.get("coins", {})
        fg = crypto.get("fear_greed", {})
        facts = []
        for cid, c in coins.items():
            if c.get("price") is not None and c.get("pct_change_24h") is not None:
                facts.append(f"{c.get('symbol', cid)} {format_currency(c['price'])} ({format_pct(c['pct_change_24h'])})")
        fg_label = fg.get("label", "n/a")
        prompt = f"Résume en ~50 mots la dynamique crypto. Fear & Greed: {fg_label}. Données: {', '.join(facts) or 'aucune'}."
        body = synthesize_section(prompt, config=config, system=DAILY_SYSTEM_PROMPT)
        return build_section("Crypto Pulse", body)

    def _pea_section(data: dict, config: Config) -> str:
        pea = data.get("pea") or {}
        if pea.get("source_failed"):
            return build_section("PEA Alert", "Données PEA indisponibles ce matin.")
        prices = pea.get("prices", {})
        eligibility_changed = pea.get("eligibility_changed", False)
        facts = []
        for sym, p in prices.items():
            if p.get("price") is not None and p.get("pct_change") is not None:
                facts.append(f"{sym} {format_currency(p['price'], symbol='€')} ({format_pct(p['pct_change'])})")
        alert = "ALERTE: changement d'éligibilité PEA détecté." if eligibility_changed else ""
        prompt = (
            f"Résume en ~50 mots la situation PEA France. {alert} Données: {', '.join(facts) or 'aucune'}."
        )
        body = synthesize_section(prompt, config=config, system=DAILY_SYSTEM_PROMPT)
        # Garantir le mot-clé "alerte" ou "changement" si eligibility_changed (cf. test 7)
        if eligibility_changed and "alerte" not in body.lower() and "changement" not in body.lower():
            body = "ALERTE — changement d'éligibilité PEA détecté. " + body
        return build_section("PEA Alert", body)

    def _news_section(data: dict, config: Config) -> str:
        news = data.get("news") or {}
        if news.get("source_failed"):
            return build_section("News Feed", "Headlines indisponibles ce matin.")
        headlines = news.get("headlines", [])[:7]
        if not headlines:
            return build_section("News Feed", "Aucun titre récent disponible.")
        bullets = "\n".join(f"- {h.get('title', 'titre manquant')} ({h.get('source', '?')})" for h in headlines)
        prompt = f"Liste structurée des titres financiers du matin :\n{bullets}\nRésume en ~40 mots les 2-3 thèmes dominants."
        body = synthesize_section(prompt, config=config, system=DAILY_SYSTEM_PROMPT)
        return build_section("News Feed", f"{bullets}\n\n{body}")

    def _one_signal_section(data: dict, config: Config) -> str:
        meta = data.get("_meta", {})
        prompt = (
            "Identifie LE point d'attention principal du matin pour un investisseur particulier "
            "français exposé ETF/crypto/PEA. Réponse en ~40 mots, ton analyste, sans conseil financier explicite. "
            f"Sources OK: {meta.get('sources_ok', [])}. Sources failed: {meta.get('sources_failed', [])}."
        )
        body = synthesize_section(prompt, config=config, system=DAILY_SYSTEM_PROMPT)
        return build_section("One Signal", body)

    def build_daily_report(data: dict, config: Config) -> str:
        """
        Construit le Daily Report (~300 mots, 6 sections).

        Args:
            data: dict produit par collect_all() — clés etf/crypto/pea/macro/news/_meta
            config: Config (utilisé pour anthropic_model et anthropic_api_key)

        Returns:
            Rapport Markdown complet (chaîne) — toujours 6 sections, jamais lève.
        """
        try:
            sections = [
                _macro_section(data, config),
                _etf_section(data, config),
                _crypto_section(data, config),
                _pea_section(data, config),
                _news_section(data, config),
                _one_signal_section(data, config),
            ]
            return "\n".join(sections)
        except Exception as e:
            logger.error("build_daily_report failed: %s", e)
            # Filet de sécurité — retourner les 6 titres en mode dégradé total
            return "\n".join(
                build_section(t, "[Section indisponible.]")
                for t in ("Macro Snapshot", "ETF Radar", "Crypto Pulse", "PEA Alert", "News Feed", "One Signal")
            )
    ```

    Important : si les tests utilisent `data["pea"]["source_failed"]` ou un autre champ, ajuster le code pour matcher EXACTEMENT le contrat de Task 1. Les tests sont la source de vérité.

    Lancer `python3 -m pytest tests/test_daily.py -v` — les 7 tests doivent passer.
    Lancer `python3 -m pytest tests/ -v` — la suite complète doit rester verte (≥ 55 tests).

    Commit avec message : `feat(03-02): implement reporters/daily.py — daily report builder, 6 sections, ~300 words`.
  </action>
  <verify>
    <automated>cd /home/crypton/cryptolascar &amp;&amp; python3 -m pytest tests/test_daily.py -v 2>&amp;1 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `reporters/daily.py` existe : `test -f reporters/daily.py` exit 0
    - La fonction publique est exportée : `grep -c "^def build_daily_report" reporters/daily.py` >= 1
    - Les 6 titres de section sont produits : `grep -cE "build_section\\(['\"]Macro Snapshot|build_section\\(['\"]ETF Radar|build_section\\(['\"]Crypto Pulse|build_section\\(['\"]PEA Alert|build_section\\(['\"]News Feed|build_section\\(['\"]One Signal" reporters/daily.py` >= 6
    - synthesize_section est utilisé (pas de narration hardcodée) : `grep -c "synthesize_section(" reporters/daily.py` >= 5
    - Aucun nom de modèle Claude hardcodé : `grep -E "['\"]claude-[a-z0-9-]+['\"]" reporters/daily.py | grep -v '^\\s*#'` n'a aucun match
    - Les 7 tests passent : `python3 -m pytest tests/test_daily.py 2>&amp;1 | tail -3 | grep -E "7 passed"` exit 0
    - La suite complète reste verte : `python3 -m pytest tests/ 2>&amp;1 | tail -3 | grep -E "passed" | grep -v "failed"` exit 0
    - Smoke test 300 mots : `python3 -c "import sys; sys.path.insert(0,'.'); from unittest.mock import patch; from tests.test_integration import MOCK_ETF_OK, MOCK_CRYPTO_OK, MOCK_PEA_OK, MOCK_MACRO_OK, MOCK_NEWS_OK; from config import Config; from pathlib import Path; cfg=Config(smtp_host='x',smtp_port=1,smtp_user='x',smtp_password='x',recipient_list=['x'],anthropic_api_key='x',anthropic_model='claude-sonnet-4-6',coingecko_api_key='',alpha_vantage_key='',fred_api_key='',newsapi_key='',db_path=Path('/tmp/x.db'),log_level='INFO',log_file=''); data={'etf':MOCK_ETF_OK,'crypto':MOCK_CRYPTO_OK,'pea':MOCK_PEA_OK,'macro':MOCK_MACRO_OK,'news':MOCK_NEWS_OK,'_meta':{'sources_ok':[],'sources_failed':[]}};
with patch('reporters.daily.synthesize_section', return_value='Lorem ipsum dolor sit amet ' * 8):
    from reporters.daily import build_daily_report
    r=build_daily_report(data, cfg)
    assert 'Macro Snapshot' in r and 'One Signal' in r
    print('OK')"` exit 0
    - Le commit GREEN existe : `git log --oneline -1 | grep -E "feat\\(03-02\\)"` exit 0
  </acceptance_criteria>
  <done>reporters/daily.py implémenté ; 7/7 tests test_daily.py passent ; suite complète reste verte ; commit `feat(03-02): ...` créé.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data dict (collect_all) → reporters/daily.py | Données potentiellement contrôlées partiellement par sources externes (titres news scrapés, etc.) ; injectées dans des prompts Claude. |
| reporters/daily.py → reporters/base.synthesize_section | Délègue tout appel API ; héritage des mitigations T-03-01/T-03-02 du Plan 03-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Headlines news utilisés dans le prompt Claude | accept | Les titres scrapés sont concaténés dans le prompt utilisateur (pas système). Risque de prompt injection limité (Claude ne génère que du texte, pas d'exécution). À ré-évaluer Phase 4 si tweet dérivé. |
| T-03-06 | Denial of Service | data dict avec subsections manquantes (source_failed) | mitigate | Chaque sous-fonction `_xxx_section` checke `source_failed` et retourne un message "indisponible" plutôt que de planter. Test 6 vérifie. |
| T-03-07 | Information Disclosure | Logs lors d'erreur de build | mitigate | `logger.error("build_daily_report failed: %s", e)` log uniquement le message d'erreur ; jamais le `config` complet (qui contiendrait `anthropic_api_key`). |

</threat_model>

<verification>
- `python3 -m pytest tests/test_daily.py -v` → 7 tests passent
- `python3 -m pytest tests/` → ≥ 55 tests passent (48 antérieurs + 7 nouveaux, total dépend du plan 03-03/04 ordre)
- `grep "synthesize_section" reporters/daily.py` ≥ 5 occurrences (preuve qu'au moins 5 sections passent par le client Claude)
- Smoke test : appel `build_daily_report` avec mocks ⇒ chaîne ≥ 250 mots ≤ 400, contient les 6 titres.
</verification>

<success_criteria>
- `reporters/daily.py` existe et exporte `build_daily_report(data, config) -> str`
- Le rapport produit a 6 sections aux titres exacts définis par REPT-01
- Le compte de mots est dans la fourchette cible 250–400 (~300)
- Les sections narratives sont déléguées à `synthesize_section` (testable, mockable)
- Le rapport reste structuré même si Claude est indisponible (FALLBACK_TEMPLATE intégré)
- Le rapport reste structuré même si une source de données est en échec (`source_failed: True`)
- Tests TDD : RED puis GREEN dans deux commits séparés
</success_criteria>

<output>
After completion, create `.planning/phases/03-report-generation/03-02-SUMMARY.md`.
</output>
