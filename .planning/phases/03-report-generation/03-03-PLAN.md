---
phase: 03-report-generation
plan: "03-03"
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - reporters/weekly.py
  - tests/test_weekly.py
autonomous: true
requirements:
  - REPT-02
must_haves:
  truths:
    - "Appeler `build_weekly_report(data)` avec un dict de fixtures retourne une chaîne ≥ 650 mots et ≤ 1000 mots"
    - "Le rapport contient exactement 7 sections au titre Markdown `## ` (Weekly Wrap)"
    - "Au moins une section contient un tableau Markdown (lignes commençant par `|`) — les données quantitatives sont tabulées"
    - "Les sections narratives passent par `reporters.base.synthesize_section`"
    - "Si l'API Claude échoue, le rapport reste à 7 sections (dégradation gracieuse)"
  artifacts:
    - path: "reporters/weekly.py"
      provides: "Weekly Wrap builder ~800 mots, 7 sections + tableaux"
      exports: ["build_weekly_report"]
    - path: "tests/test_weekly.py"
      provides: "Tests TDD pour reporters/weekly.py"
      contains: "def test_build_weekly_report"
  key_links:
    - from: "reporters/weekly.py"
      to: "reporters.base.synthesize_section"
      via: "import direct"
      pattern: "from reporters\\.base import"
    - from: "reporters/weekly.py"
      to: "data dict (from collect_all)"
      via: "consomme etf/crypto/pea/macro/news"
      pattern: "data\\[['\"](etf|crypto|pea|macro|news)['\"]\\]"
---

<objective>
Construire `reporters/weekly.py` qui prend le dict de données et retourne un Weekly Wrap d'environ 800 mots structuré en 7 sections avec tableaux de données. Format dominical (publié le dimanche), agrège la semaine écoulée. Sections narratives via `synthesize_section`. Tableaux Markdown pour les données chiffrées.

Purpose: Le Weekly Wrap est le rendez-vous hebdomadaire. Format plus narratif et plus dense que le Daily, avec des tableaux pour appuyer l'analyse. REPT-02 spécifie ~800 mots et 7 sections.

Output: `reporters/weekly.py` testé (RED+GREEN), exposant `build_weekly_report(data: dict, config: Config) -> str`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@tests/test_integration.py
@reporters/base.py

<interfaces>
<!-- Même contrat data que le Plan 03-02 (cf. tests/test_integration.py MOCK_*_OK) -->
<!-- Mêmes helpers reporters.base : synthesize_section, build_section, format_pct, format_currency, FALLBACK_TEMPLATE -->

Sections requises pour le Weekly Wrap (7 sections, ordre suggéré par cohérence avec le Daily) :
1. Executive Summary — narration ~120 mots, vue d'ensemble de la semaine
2. Macro Watch — taux, inflation, M2 + tableau
3. ETF Performance — tableau de performance + narration
4. Crypto Recap — tableau des coins + narration + Fear & Greed
5. PEA Wrap — tableau prix PEA + alerte si eligibility_changed
6. News Digest — top 8-10 headlines (tabulés ou listés)
7. Outlook — narration ~120 mots, points d'attention pour la semaine à venir

Format tableau Markdown attendu :
```
| Ticker | Prix | Variation |
|--------|------|-----------|
| SPY | $500.00 | +1.01% |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Écrire les tests TDD pour reporters/weekly.py</name>
  <files>tests/test_weekly.py</files>
  <read_first>
    - tests/test_integration.py (MOCK_*_OK pour data fixture)
    - tests/test_macro.py (pattern fixture tmp_config)
    - reporters/base.py (signatures helpers)
  </read_first>
  <behavior>
    - Test 1 (test_build_weekly_report_returns_seven_sections): mocker `synthesize_section` ; assert que la sortie contient au moins 7 lignes commençant par `"## "`. Compter via `report.count("\n## ")` + 1 si commence par `## `.
    - Test 2 (test_build_weekly_report_word_count_in_target_range): mock `synthesize_section` retourne ~100 mots ; assert 650 ≤ word_count ≤ 1000 (cible 800, tolérance ~20%).
    - Test 3 (test_build_weekly_report_contains_markdown_tables): assert que la sortie contient au moins UNE ligne matchant le pattern `|.*|.*|` (tableau Markdown) — preuve que les données chiffrées sont tabulées.
    - Test 4 (test_build_weekly_report_etf_table_lists_all_tickers): construire data avec MOCK_ETF_OK étendu (5 tickers SPY, QQQ, IWDA.AS, EIMI.AS, CSPX.AS) ; assert que chaque symbol apparaît dans la sortie.
    - Test 5 (test_build_weekly_report_calls_synthesize_section_at_least_three_times): patch ; assert `mock.call_count >= 3` (au moins 3 sections narratives).
    - Test 6 (test_build_weekly_report_handles_partial_data): passer `data["pea"] = {"error": "...", "source_failed": True}` ; assert que la section PEA Wrap existe avec un message indiquant l'indisponibilité, et que les autres sections sont normales.
    - Test 7 (test_build_weekly_report_uses_configured_anthropic_model): inspecter `mock.call_args_list` ; assert que tous les appels reçoivent `config.anthropic_model == "claude-sonnet-4-6"` (preuve LLM-02).
    - Test 8 (test_build_weekly_report_never_raises): passer `data = {}` (totalement vide) ; assert pas d'exception, retourne quand même 7 sections (dégradation totale).
  </behavior>
  <action>
    Créer `tests/test_weekly.py`. Patron similaire à `tests/test_daily.py` (Plan 03-02).

    Réutiliser MOCK_*_OK depuis `tests/test_integration.py`. Étendre `MOCK_ETF_OK["tickers"]` pour couvrir les 5 ETFs (Test 4) — soit dans une fixture locale, soit en construisant la data au cas par cas.

    Imports : `from reporters.weekly import build_weekly_report`.

    Pour Test 3, utiliser : `assert any('|' in line and line.count('|') >= 2 for line in report.splitlines())`.

    Pour Test 8, accepter le contrat de dégradation totale : `data = {}` ⇒ chaque section affiche "indisponible" mais retour string non vide avec 7 titres `## `.

    Lancer `python3 -m pytest tests/test_weekly.py -v` — doit échouer en ImportError (RED).

    Commit avec message : `test(03-03): add failing tests for reporters/weekly.py`.
  </action>
  <verify>
    <automated>cd /home/crypton/cryptolascar &amp;&amp; python3 -m pytest tests/test_weekly.py -v 2>&amp;1 | grep -E "ImportError|ModuleNotFoundError|collected 8"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `tests/test_weekly.py` existe : `test -f tests/test_weekly.py` exit 0
    - Au moins 8 tests sont déclarés : `grep -c "^def test_" tests/test_weekly.py` >= 8
    - L'import du symbole cible est présent : `grep -c "from reporters.weekly import build_weekly_report" tests/test_weekly.py` >= 1
    - Test sur les 7 sections présent : `grep -c '"## "' tests/test_weekly.py` >= 1 OR `grep -c "count(.\\?## " tests/test_weekly.py` >= 1
    - Test sur tableau Markdown présent : `grep -E "'|'.*split|'.*\\|.*\\|'" tests/test_weekly.py` retourne au moins 1 ligne
    - Les tests échouent en RED : `python3 -m pytest tests/test_weekly.py 2>&amp;1 | grep -E "ModuleNotFoundError|ImportError"` exit 0
    - Le commit RED existe : `git log --oneline -1 | grep -E "test\\(03-03\\)"` exit 0
  </acceptance_criteria>
  <done>tests/test_weekly.py contient 8 tests qui échouent tous avec ImportError ; commit `test(03-03): ...` créé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implémenter reporters/weekly.py</name>
  <files>reporters/weekly.py</files>
  <read_first>
    - tests/test_weekly.py (contrat à satisfaire)
    - reporters/base.py (helpers)
    - reporters/daily.py (référence du pattern de découpage en sections — déjà construit Plan 03-02)
    - tests/test_integration.py (structure data)
  </read_first>
  <action>
    Créer `reporters/weekly.py` exportant `build_weekly_report(data: dict, config: Config) -> str`.

    Squelette imposé :

    ```python
    """
    reporters/weekly.py — Weekly Wrap builder (~800 mots, 7 sections + tableaux).

    Sections (REPT-02) :
      1. Executive Summary  — narration vue d'ensemble (~120 mots)
      2. Macro Watch        — tableau taux/inflation + narration
      3. ETF Performance    — tableau performance + narration
      4. Crypto Recap       — tableau coins + Fear & Greed + narration
      5. PEA Wrap           — tableau PEA + alerte éligibilité
      6. News Digest        — bullets top headlines
      7. Outlook            — narration semaine à venir (~120 mots)
    """
    from __future__ import annotations
    from config import Config
    from reporters.base import synthesize_section, build_section, format_pct, format_currency
    from logging_setup import get_logger

    logger = get_logger(__name__)

    WEEKLY_SYSTEM_PROMPT = (
        "Tu es un analyste financier français. Réponds en français, ton sobre et factuel. "
        "Sections de ~100-120 mots chacune. Pas de conseil financier explicite."
    )

    def _table(headers: list[str], rows: list[list[str]]) -> str:
        """Construit un tableau Markdown."""
        if not rows:
            return ""
        head = "| " + " | ".join(headers) + " |"
        sep = "|" + "|".join(["---"] * len(headers)) + "|"
        body = "\n".join("| " + " | ".join(r) + " |" for r in rows)
        return f"{head}\n{sep}\n{body}"

    def _executive_summary(data: dict, config: Config) -> str:
        meta = data.get("_meta", {}) or {}
        prompt = (
            f"Rédige le résumé exécutif (~120 mots) de la semaine financière. "
            f"Sources OK: {meta.get('sources_ok', [])}. "
            f"Sources failed: {meta.get('sources_failed', [])}."
        )
        body = synthesize_section(prompt, config=config, system=WEEKLY_SYSTEM_PROMPT)
        return build_section("Executive Summary", body)

    def _macro_watch(data: dict, config: Config) -> str:
        macro = data.get("macro") or {}
        if macro.get("source_failed"):
            return build_section("Macro Watch", "Données macro indisponibles cette semaine.")
        series = macro.get("series", {})
        rows = [[k, str(s.get("value", "n/a")), str(s.get("date", "n/a"))]
                for k, s in series.items() if isinstance(s, dict)]
        table = _table(["Série", "Valeur", "Date"], rows)
        prompt = f"Commentaire macro ~100 mots à partir des séries: {list(series.keys())}."
        body = synthesize_section(prompt, config=config, system=WEEKLY_SYSTEM_PROMPT)
        return build_section("Macro Watch", f"{table}\n\n{body}" if table else body)

    def _etf_performance(data: dict, config: Config) -> str:
        etf = data.get("etf") or {}
        if etf.get("source_failed"):
            return build_section("ETF Performance", "Données ETF indisponibles.")
        tickers = etf.get("tickers", {})
        rows = []
        for sym, t in tickers.items():
            if t.get("price") is not None:
                rows.append([sym, format_currency(t["price"]),
                             format_pct(t.get("pct_change", 0.0))])
        table = _table(["Ticker", "Prix", "Variation"], rows)
        prompt = f"Commentaire ETF ~100 mots. Tickers couverts: {list(tickers.keys())}."
        body = synthesize_section(prompt, config=config, system=WEEKLY_SYSTEM_PROMPT)
        return build_section("ETF Performance", f"{table}\n\n{body}" if table else body)

    def _crypto_recap(data: dict, config: Config) -> str:
        crypto = data.get("crypto") or {}
        if crypto.get("source_failed"):
            return build_section("Crypto Recap", "Données crypto indisponibles.")
        coins = crypto.get("coins", {})
        fg = crypto.get("fear_greed", {}) or {}
        rows = []
        for cid, c in coins.items():
            if c.get("price") is not None:
                rows.append([c.get("symbol", cid), format_currency(c["price"]),
                             format_pct(c.get("pct_change_24h", 0.0))])
        table = _table(["Coin", "Prix", "24h"], rows)
        prompt = (f"Commentaire crypto ~100 mots. Fear & Greed: {fg.get('label', 'n/a')} "
                  f"({fg.get('value', 'n/a')}). Coins: {list(coins.keys())}.")
        body = synthesize_section(prompt, config=config, system=WEEKLY_SYSTEM_PROMPT)
        return build_section("Crypto Recap", f"{table}\n\n{body}" if table else body)

    def _pea_wrap(data: dict, config: Config) -> str:
        pea = data.get("pea") or {}
        if pea.get("source_failed"):
            return build_section("PEA Wrap", "Données PEA indisponibles.")
        prices = pea.get("prices", {})
        rows = []
        for sym, p in prices.items():
            if p.get("price") is not None:
                rows.append([sym, format_currency(p["price"], symbol="€"),
                             format_pct(p.get("pct_change", 0.0))])
        table = _table(["Ticker", "Prix", "Variation"], rows)
        alert = ""
        if pea.get("eligibility_changed"):
            alert = "ALERTE — changement d'éligibilité PEA détecté.\n\n"
        prompt = f"Commentaire PEA France ~100 mots. Tickers: {list(prices.keys())}."
        body = synthesize_section(prompt, config=config, system=WEEKLY_SYSTEM_PROMPT)
        return build_section("PEA Wrap", f"{alert}{table}\n\n{body}" if table else f"{alert}{body}")

    def _news_digest(data: dict, _config: Config) -> str:
        news = data.get("news") or {}
        if news.get("source_failed"):
            return build_section("News Digest", "Headlines indisponibles cette semaine.")
        headlines = news.get("headlines", [])[:10]
        if not headlines:
            return build_section("News Digest", "Aucun titre récent disponible.")
        bullets = "\n".join(f"- **{h.get('source', '?')}** — {h.get('title', 'titre manquant')}"
                            for h in headlines)
        return build_section("News Digest", bullets)

    def _outlook(data: dict, config: Config) -> str:
        prompt = (
            "Rédige les perspectives (~120 mots) pour la semaine financière à venir. "
            "Mentionne 2-3 points d'attention pour un investisseur particulier français "
            "exposé ETF mondiaux, crypto et PEA."
        )
        body = synthesize_section(prompt, config=config, system=WEEKLY_SYSTEM_PROMPT)
        return build_section("Outlook", body)

    def build_weekly_report(data: dict, config: Config) -> str:
        """
        Construit le Weekly Wrap (~800 mots, 7 sections + tableaux).

        Args:
            data: dict produit par collect_all() (peut être vide ou partiel)
            config: Config avec anthropic_api_key et anthropic_model

        Returns:
            Rapport Markdown complet — toujours 7 sections, jamais lève.
        """
        try:
            sections = [
                _executive_summary(data, config),
                _macro_watch(data, config),
                _etf_performance(data, config),
                _crypto_recap(data, config),
                _pea_wrap(data, config),
                _news_digest(data, config),
                _outlook(data, config),
            ]
            return "\n".join(sections)
        except Exception as e:
            logger.error("build_weekly_report failed: %s", e)
            return "\n".join(
                build_section(t, "[Section indisponible.]")
                for t in ("Executive Summary", "Macro Watch", "ETF Performance",
                          "Crypto Recap", "PEA Wrap", "News Digest", "Outlook")
            )
    ```

    Lancer `python3 -m pytest tests/test_weekly.py -v` — 8 tests doivent passer.
    Lancer `python3 -m pytest tests/ -v` — la suite complète reste verte.

    Commit avec message : `feat(03-03): implement reporters/weekly.py — weekly wrap, 7 sections, ~800 words with tables`.
  </action>
  <verify>
    <automated>cd /home/crypton/cryptolascar &amp;&amp; python3 -m pytest tests/test_weekly.py -v 2>&amp;1 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `reporters/weekly.py` existe : `test -f reporters/weekly.py` exit 0
    - La fonction publique est exportée : `grep -c "^def build_weekly_report" reporters/weekly.py` >= 1
    - 7 titres de section produits : `grep -cE "build_section\\(['\"](Executive Summary|Macro Watch|ETF Performance|Crypto Recap|PEA Wrap|News Digest|Outlook)" reporters/weekly.py` >= 7
    - synthesize_section appelé pour ≥ 5 sections : `grep -c "synthesize_section(" reporters/weekly.py` >= 5
    - Au moins une fonction _table pour les tableaux : `grep -c "^def _table" reporters/weekly.py` >= 1
    - Aucun nom de modèle Claude hardcodé : `grep -E "['\"]claude-[a-z0-9-]+['\"]" reporters/weekly.py | grep -v '^\\s*#'` n'a aucun match
    - Les 8 tests passent : `python3 -m pytest tests/test_weekly.py 2>&amp;1 | tail -3 | grep -E "8 passed"` exit 0
    - La suite complète reste verte : `python3 -m pytest tests/ 2>&amp;1 | tail -3 | grep "failed" | grep -E "^0 failed| 0 failed"` OR `python3 -m pytest tests/ 2>&amp;1 | tail -3 | grep -v "failed"` indique passed
    - Le commit GREEN existe : `git log --oneline -1 | grep -E "feat\\(03-03\\)"` exit 0
  </acceptance_criteria>
  <done>reporters/weekly.py implémenté ; 8/8 tests test_weekly.py passent ; suite complète reste verte ; commit `feat(03-03): ...` créé.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data dict (collect_all) → reporters/weekly.py | Mêmes données que daily ; subsections potentiellement partielles. |
| reporters/weekly.py → reporters/base.synthesize_section | Délègue les appels Claude ; héritage T-03-01/T-03-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | Headlines news inclus en bullets dans le tableau | accept | Les titres scrapés sont rendus en Markdown bullets ; pas exécutés. Risque négligeable côté backend (pas de XSS car pas de HTML rendu côté reporter — Phase 4 gérera l'encodage HTML). |
| T-03-09 | Denial of Service | data dict vide ou totalement partiel | mitigate | Filet `try/except` global dans `build_weekly_report` ; chaque sous-fonction checke `source_failed` ; data={} retourne 7 sections "[Section indisponible.]" (Test 8). |
| T-03-10 | Information Disclosure | Tableaux Markdown | accept | Les tableaux contiennent des données de marché publiques (prix, variations) ; aucune donnée personnelle. |

</threat_model>

<verification>
- `python3 -m pytest tests/test_weekly.py -v` → 8 tests passent
- `python3 -m pytest tests/` → suite complète verte
- Smoke test : appel `build_weekly_report` avec mocks complets ⇒ 7 sections, ≥ 1 tableau Markdown, 650 ≤ word_count ≤ 1000.
- `grep -c "synthesize_section(" reporters/weekly.py` ≥ 5
</verification>

<success_criteria>
- `reporters/weekly.py` existe et exporte `build_weekly_report(data, config) -> str`
- Le rapport produit a 7 sections (titres `## `)
- Au moins une section contient un tableau Markdown (lignes `|...|...|`)
- Le compte de mots est dans la fourchette 650–1000 (~800)
- Les sections narratives passent par `synthesize_section`
- Le rapport reste structuré même avec `data = {}` (dégradation totale)
- Tests TDD : RED puis GREEN dans deux commits séparés
</success_criteria>

<output>
After completion, create `.planning/phases/03-report-generation/03-03-SUMMARY.md`.
</output>
