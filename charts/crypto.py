"""
charts/crypto.py — Crypto sparkline chart generator (CHART-02).

Stub in this worktree — implemented by plan 06-02 executor.
This file exists to allow charts/__init__.py to import without error
while plans 06-03 (gauge + PEA) execute in parallel.

Public API:
  generate_crypto_sparklines(btc_history, eth_history) -> Optional[str]
"""
from __future__ import annotations
from typing import Optional


def generate_crypto_sparklines(btc_history, eth_history) -> Optional[str]:
    """Stub — returns None. Real implementation in plan 06-02."""
    return None
