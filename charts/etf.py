"""
charts/etf.py — ETF bar chart generator (CHART-01).

Stub in this worktree — implemented by plan 06-01 executor.
This file exists to allow charts/__init__.py to import without error
while plans 06-03 (gauge + PEA) execute in parallel.

Public API:
  generate_etf_chart(etf_data, date_str) -> Optional[str]
"""
from __future__ import annotations
from typing import Optional


def generate_etf_chart(etf_data, date_str: str = "") -> Optional[str]:
    """Stub — returns None. Real implementation in plan 06-01."""
    return None
